package com.github.curriculeon;

// TODO - Parameterize method signature with `SomeType` such that `SomeType` is a subclass of `Object`
public interface PersonPropertyInterface {
    // TODO - Modify method declarations to reflect the aforementioned `SomeType`
    Object get();
    void set(Object newValueToBeSet);
    void print();
}
